{
  "name": "jnn-youth-centre-management",
  "version": "1.0.0",
  "description": "JNN Youth Centre Management System",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm install && node server.js",
    "dev:frontend": "cd frontend && npm install && npm run dev",
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && node server.js",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["education", "management", "fees", "students"],
  "author": "JNN Youth Centre",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
